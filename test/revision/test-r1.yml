# yaml-embedded-languages: sql

__namespace: test
__revision: 1

test:
  sql:
    - |
      create table test (
        id serial primary key,
        name text not null
      );

  meta:
    - up: |
        insert into test (name) values ('test1');